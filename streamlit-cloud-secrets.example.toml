# Paste into Streamlit Cloud → App settings → Secrets
# (also fine for local .streamlit/secrets.toml)

SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
SUPABASE_KEY = "your_service_role_key_here"

# Manager tab — full access (also seeds first Admin user "admin" if none exist)
APP_PASSWORD = "choose-a-strong-manager-password"

# Shared Technician tab
TECH_PASSWORD = "choose-a-shared-tech-password"
